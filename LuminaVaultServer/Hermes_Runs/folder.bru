meta {
  name: Hermes_Runs
}

auth {
  mode: inherit
}

docs {
  Agent runs on the tenant's own Hermes — start, follow live over SSE, approve or deny tool calls from a push notification, stop (Phase 1).

}
