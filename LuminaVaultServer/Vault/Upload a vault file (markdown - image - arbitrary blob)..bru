meta {
  name: Upload a vault file (markdown / image / arbitrary blob).
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/vault/files
  body: none
  auth: inherit
}

params:query {
  path: 
  ~space_id: 
}
