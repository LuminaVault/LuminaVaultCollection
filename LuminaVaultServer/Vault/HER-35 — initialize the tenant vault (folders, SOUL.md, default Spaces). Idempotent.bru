meta {
  name: HER-35 — initialize the tenant vault (folders, SOUL.md, default Spaces). Idempotent
  type: http
  seq: 7
  tags: [
    Vault
  ]
}

post {
  url: {{baseUrl}}/v1/vault/create
  body: json
  auth: inherit
}

body:json {
  {}
}

example {
  name: 200 Response
  description: Vault status after create.

  request: {
    url: {{baseUrl}}/v1/vault/create
    method: POST
    mode: json
    body:json: {
      {}
    }
  }

  response: {
    headers: {
      Content-Type: application/json
    }

    status: {
      code: 200
      text: OK
    }

    body: {
      type: json
      content: '''
        {
          "initialized": false,
          "createdAt": "",
          "defaultSpaceSlugs": []
        }
      '''
    }
  }
}
