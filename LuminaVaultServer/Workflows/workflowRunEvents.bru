meta {
  name: workflowRunEvents
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/v1/workflows/runs/:runID/events
  body: none
  auth: inherit
}

params:query {
  ~after:
}

params:path {
  runID:
}
