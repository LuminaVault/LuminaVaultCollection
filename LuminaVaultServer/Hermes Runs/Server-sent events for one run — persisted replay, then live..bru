meta {
  name: Server-sent events for one run — persisted replay, then live.
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/v1/hermes/runs/:id/events
  body: none
  auth: inherit
}

params:query {
  ~after:
}
