meta {
  name: Run the health-correlation engine across all tenants.
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v1/admin/health/correlate
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
