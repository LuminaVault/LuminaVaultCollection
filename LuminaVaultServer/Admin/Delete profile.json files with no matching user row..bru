meta {
  name: Delete profile.json files with no matching user row.
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/v1/admin/hermes-profiles/reap-orphans
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
