meta {
  name: Provider-registry health (per-provider 2xx/last-error).
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/admin/llm/health
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
